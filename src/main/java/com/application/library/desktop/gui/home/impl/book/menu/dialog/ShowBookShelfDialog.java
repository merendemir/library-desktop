package com.application.library.desktop.gui.home.impl.book.menu.dialog;

import com.application.library.desktop.constants.TitleConstants;
import com.application.library.desktop.gui.home.impl.shelf.ShelvesPanelCreateService;
import com.application.library.desktop.gui.home.impl.shelf.ShowShelvesPanel;
import com.application.library.desktop.request.view.shelf.ShelfDTO;
import com.application.library.desktop.utils.ApplicationContextHelper;
import com.intellij.uiDesigner.core.GridConstraints;
import com.intellij.uiDesigner.core.GridLayoutManager;
import org.springframework.stereotype.Service;

import javax.swing.*;
import java.awt.*;

@Service
public class ShowBookShelfDialog extends JDialog {

    private ShelfDTO selectedShelf;

    private final ShowShelvesPanel showShelvesPanel;
    private final ApplicationContextHelper applicationContextHelper;

    public ShowBookShelfDialog(ShelvesPanelCreateService shelvesPanelCreateService, ApplicationContextHelper applicationContextHelper) {
        this.showShelvesPanel = shelvesPanelCreateService.createNewShelvesPanel(this::fillShelvesPanel);
        this.applicationContextHelper = applicationContextHelper;
        $$$setupUI$$$();

        setTitle(TitleConstants.SHOW_SHELF);
        setSize(400, 100);
        setContentPane(contentPane);
        showShelvesPanel.showPaginationPanel(false);
        setModal(true);
    }

    public void showDialog(ShelfDTO shelfDTO) {
        this.selectedShelf = shelfDTO;
        setLocationRelativeTo(applicationContextHelper.getCurrentFrame());
        showShelvesPanel.selected();
        setVisible(true);
    }

    //COMPONENTS
    private JPanel contentPane;
    private JPanel showShelfPanelGUI;

    private void createUIComponents() {
        showShelfPanelGUI = showShelvesPanel;
    }

    private void fillShelvesPanel() {
        showShelvesPanel.removeAllRows();
        showShelvesPanel.addRow(selectedShelf);
    }

    /**
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    private void $$$setupUI$$$() {
        createUIComponents();
        contentPane = new JPanel();
        contentPane.setLayout(new GridLayoutManager(1, 1, new Insets(10, 10, 10, 10), -1, -1));
        contentPane.add(showShelfPanelGUI, new GridConstraints(0, 0, 1, 1, GridConstraints.ANCHOR_CENTER, GridConstraints.FILL_BOTH, GridConstraints.SIZEPOLICY_CAN_SHRINK | GridConstraints.SIZEPOLICY_CAN_GROW, GridConstraints.SIZEPOLICY_CAN_SHRINK | GridConstraints.SIZEPOLICY_CAN_GROW, null, null, null, 0, false));
    }

    /**
     * @noinspection ALL
     */
    public JComponent $$$getRootComponent$$$() {
        return contentPane;
    }
}
