package com.application.library.desktop.gui.header;

import com.application.library.desktop.enumerations.NotificationType;
import com.application.library.desktop.gui.header.enumeration.ApplicationHeaderCard;
import com.application.library.desktop.gui.header.impl.HeaderPanel;
import com.application.library.desktop.gui.header.impl.NotificationPanel;

import javax.swing.*;
import java.awt.*;

public class ApplicationHeaderPanel extends JPanel {
    private final HeaderPanel headerPanel = new HeaderPanel();
    private final NotificationPanel notificationPanel = new NotificationPanel();

    public ApplicationHeaderPanel() {
        $$$setupUI$$$();
        setLayout(new BorderLayout());
        add(contentPane, BorderLayout.CENTER);
        updateCardLayout(ApplicationHeaderCard.HEADER_PANEL);
    }

    public void showNotification(String message, NotificationType type) {
        updateCardLayout(ApplicationHeaderCard.NOTIFICATION_PANEL);
        notificationPanel.showNotification(message, type);
    }

    public void hideNotification() {
        updateCardLayout(ApplicationHeaderCard.HEADER_PANEL);
    }

    private void updateCardLayout(ApplicationHeaderCard card) {
        CardLayout cardLayout = (CardLayout) contentPane.getLayout();
        cardLayout.show(contentPane, card.getCardName());
    }


    private void createUIComponents() {
        headerPanelGUI = headerPanel;
        notificationPanelGUI = notificationPanel;
    }

    //COMPONENTS
    private JPanel contentPane;
    private JPanel notificationPanelGUI;
    private JPanel headerPanelGUI;

    /**
     * Method generated by IntelliJ IDEA GUI Designer
     * >>> IMPORTANT!! <<<
     * DO NOT edit this method OR call it in your code!
     *
     * @noinspection ALL
     */
    private void $$$setupUI$$$() {
        createUIComponents();
        contentPane = new JPanel();
        contentPane.setLayout(new CardLayout(0, 0));
        contentPane.add(notificationPanelGUI, "notificationCard");
        contentPane.add(headerPanelGUI, "headerCard");
    }

    /**
     * @noinspection ALL
     */
    public JComponent $$$getRootComponent$$$() {
        return contentPane;
    }

}
